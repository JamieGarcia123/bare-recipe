[build]
  command = "npm run build"    # build command runs Next.js build
  publish = "out"              # folder generated by `output: 'export'`

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
